# Lexer and vocabulary

The lexer and vocabulary are configured via `lexer.yaml`; the file format and top-level keys are described in [Configuration](configuration.md#lexeryaml-io-and-vocabulary-iopipeline). Below are the API references for the classes that consume that configuration.

<!-- ## Lexer -->

::: calt.io.preprocessor.UnifiedLexer
    options:
      members: ["__init__"]
      heading: "UnifiedLexer"
::: calt.io.preprocessor.NumberPolicy
    options:
      members: ["__init__"]
      heading: "NumberPolicy"

<!-- ## Vocabulary -->

::: calt.io.vocabulary.config.VocabConfig
    options:
      members: ["__init__"]
      heading: "VocabConfig"
